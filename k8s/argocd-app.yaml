apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mymel-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/vladopag/mymel.git'
    targetRevision: main
    path: k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: mymel
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
